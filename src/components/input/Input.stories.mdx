import {
  Canvas,
  Meta,
  Story,
  Props,
  ArgsTable,
} from "@storybook/addon-docs/blocks";
import Input from "./Input";

<Meta
  title="components/Input"
  component={Input}
  argTypes={{
    value: {
      description: "input에 입력되는 value값",
      defaultValue: "",
      table: {
        category: null,
      },
    },
    placeholder: {
      description: "input placeholder",
      defaultValue: "값을 입력해주세요.",
      table: {
        category: null,
      },
    },
    type: {
      description: "input의 type",
      defaultValue: "text",
      table: {
        category: null,
      },
    },
    isValidate: {
      description:
        "success, danger를 구별하기 위한 변수중 하나 type변수와 함께 사용한다.",
      defaultValue: false,
      table: {
        category: null,
      },
    },
    disabled: {
      description: "disabled 효과 여부",
      defaultValue: false,
      table: {
        category: null,
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  data() {
    return {
      inputText: "",
    };
  },
  components: { Input },
  template: '<c-input v-bind="$props" v-model="inputText"/>',
});

# Input

**input** 컴포넌트

<Canvas>
  <Story name="default" height="100px">
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
