import {
  Canvas,
  Meta,
  Story,
  Props,
  ArgsTable,
} from "@storybook/addon-docs/blocks";
import Switch from "./Switch";

<Meta
  title="components/Switch"
  component={Switch}
  argTypes={{
    size: {
      description:
        "스위치의 크기를 정할 수 있다. 기본은 `medium`이며 `small` `large`가 있다.",
      defaultValue: "medium",
      control: {
        type: "select",
        options: ["small", "medium", "large"],
      },
      table: {
        category: null,
        type: {
          summary: null,
        },
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Switch },
  template: '<c-switch v-bind="$props"/>',
});

# Switch

**switch** 컴포넌트

<Canvas>
  <Story name="default" height="100px">
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
