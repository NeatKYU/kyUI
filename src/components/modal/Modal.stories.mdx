import {
  Canvas,
  Meta,
  Story,
  Props,
  ArgsTable,
} from "@storybook/addon-docs/blocks";
import Modal from "./Modal";

<Meta
  title="components/Modal"
  component={Modal}
  argTypes={{
    titleText: {
      description: "modal의 title",
      defaultValue: "",
      table: {
        category: null,
      },
    },
    isOpen: {
      description: "modal 오픈 여부",
      defaultValue: false,
      table: {
        category: null,
      },
    },
    isCloseButton: {
      description: "modal의 close버튼 생성여부",
      defaultValue: false,
      table: {
        category: null,
      },
    },
    isAnimation: {
      description: "modal의 애니메이션 여부",
      defaultValue: false,
      table: {
        category: null,
      },
    },
    isDivider: {
      description: "modal의 구분선",
      defaultValue: false,
      table: {
        category: null,
      },
    },
    size: {
      description: "modal 크기",
      defaultValue: "sm",
      control: {
        type: "select",
        options: ["sm", "md", "lg"],
      },
      table: {
        category: null,
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Modal },
  data() {
    return {
      isOpen: false,
    };
  },
  methods: {
    openModal() {
      this.isOpen = true;
    },
    closeModal() {
      this.isOpen = false;
    },
  },
  template: `
        <c-modal v-bind="$props" :isOpen="true" @eClose="closeModal">
            <template v-slot:header>
                <span>this is modal title</span>
            </template>
            <template v-slot:footer>
                <c-button @click="closeModal">ok</c-button>
                <c-button @click="closeModal">cancel</c-button>
            </template>
        </c-modal>
        `,
});

# Modal

**modal** 컴포넌트

<Canvas>
  <Story name="default" height="100px">
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
