import {
  Canvas,
  Meta,
  Story,
  Props,
  ArgsTable,
} from "@storybook/addon-docs/blocks";
import { screen } from "@storybook/testing-library";
import { expect } from "@storybook/jest";
import RadioGroup from "./RadioGroup";

<Meta
  title="components/Radio/Group"
  component={RadioGroup}
  argTypes={{
    radioList: {
      description: "group에 들어가는 라디오 리스트",
      defaultValue: ["radio1", "radio2", "radio3"],
      table: {
        category: null,
      },
    },
    isButton: {
      description: "라디오에서 사용되는 buttonMode",
      defaultValue: false,
      table: {
        category: null,
      },
    },
    sort: {
      description: "라디오 그룹의 정렬 기준 `row` `column`",
      defaultValue: "row",
      table: {
        category: null,
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { RadioGroup },
  template: '<c-radio-group v-bind="$props"/>',
});

# RaioGroup

**radiogroup** 컴포넌트 입니다.

<Canvas>
  <Story name="default" height="100px">
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
