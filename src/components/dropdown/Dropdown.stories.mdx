import {
  Canvas,
  Meta,
  Story,
  Props,
  ArgsTable,
} from "@storybook/addon-docs/blocks";
import Dropdown from "./Dropdown";

<Meta
  title="components/Dropdown"
  component={Dropdown}
  argTypes={{
    dropdownList: {
      description: "드롭다운 아이템 리스트",
      defaultValue: ["dropdown test", "korea", "USA"],
      table: {
        category: null,
      },
    },
    hovered: {
      description: "hover로 리스트가 열릴지 여부",
      defaultValue: false,
      table: {
        category: null,
      },
    },
    disabled: {
      description: "disable효과 여부",
      defaultValue: false,
      table: {
        category: null,
      },
    },
    downIcon: {
      description: "오른쪽의 down시 아이콘",
      defaultValue: "caret-down",
      table: {
        category: null,
      },
    },
    upIcon: {
      description: "오른쪽의 up시 아이콘",
      defaultValue: "caret-up",
      table: {
        category: null,
      },
    },
    dropdownCustomClass: {
      description: "dropdown custom class string",
      defaultValue: "",
      table: {
        category: "Custom Class",
      },
    },
    itemCustomClass: {
      description: "dropdown item custom class string",
      defaultValue: "",
      table: {
        category: "Custom Class",
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Dropdown },
  template: '<c-dropdown v-bind="$props"></c-dropdown>',
});

# Dropdown

**dropdown** 컴포넌트

<Canvas>
  <Story name="default" height="100px">
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="default" />
